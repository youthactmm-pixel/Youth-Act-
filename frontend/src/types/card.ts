export type CardModel = {
  id: string
  title: string
  description: string
  category: string
  status: string
}

export type CardCreateInput = {
  title: string
  description: string
  category: string
  status: string
}
